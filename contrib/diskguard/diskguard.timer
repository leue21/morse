[Unit]
Description=Run diskguard regularly

[Timer]
# Checking often costs nothing; the script's own cooldown decides how often it
# is allowed to speak, so the two are tuned independently.
OnBootSec=5min
OnUnitActiveSec=15min
Persistent=true

[Install]
WantedBy=timers.target
